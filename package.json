{
  "name": "enhancer-for-physics-wallah",
  "version": "1.0.8.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:fast": "vite build",
    "zip": "powershell -Command \"Compress-Archive -Path dist/chrome/* -DestinationPath pw-enhancer-chrome.zip -Force; Compress-Archive -Path dist/firefox/* -DestinationPath pw-enhancer-firefox.zip -Force\"",
    "package": "npm run build && npm run zip",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write \"src/**/*.{ts,tsx,css,html}\""
  },
  "dependencies": {
    "@preact/signals": "^2.0.2",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.4",
    "@types/chrome": "^0.2.7",
    "@typescript-eslint/eslint-plugin": "^8.21.0",
    "@typescript-eslint/parser": "^8.21.0",
    "eslint": "^8.57.1",
    "eslint-plugin-react-hooks": "^5.1.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
